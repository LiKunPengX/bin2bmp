将包含RGB565数据的 BIN 文件拖动到bin2bmp.exe上，即可在BIN文件所在位置生成其对应的BMP文件，

注意
* 请根据自己图片的实际尺寸修改c文件中的尺寸宏定义数据
* 需要以从左到右，从上到下的顺序，将图片每个像素的rgb565颜色数据以大端模式存储在bin文件中
* 目前仅支持生成16位位图 
* 通过修改结构体中的rgb mask可以更改源颜色数据格式，如rgb555，修改color_convert函数可以适配rgb332，rgb888等格式

使用愉快^v^

designed by lkp

